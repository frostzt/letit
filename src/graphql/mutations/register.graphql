mutation Register($username: String!, $password: String!) {
  register(data: { username: $username, password: $password }) {
    user {
      id
      username
    }
    errors {
      message
      property
    }
  }
}
